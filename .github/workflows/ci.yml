name: ci

on:
  pull_request:
    branches:
      - main

jobs:
  ci:
    runs-on: ubuntu-slim
    steps:
      - uses: actions/checkout@v3
      - name: Smoke test
        env:
          XDG_CONFIG_HOME: ${{ runner.temp }}/dotfiles-config
        run: ./init
